---
mode: testing
max_steps: 50
target: chrome
---

# Session: 2026-08-21T11-52-44

## Step 1
You are testing **Weather Model Fight Club**, a web application that compares forecasts from multiple weather models/providers and determines which model performed best against observed weather conditions.

Test the application as a real user would.

## Core flow

1. Open the application and confirm the main page loads successfully.
2. Verify that the page clearly communicates what Weather Model Fight Club does.
3. Enter or select a valid location.
4. Run a weather-model comparison.
5. Confirm that forecasts from multiple models/providers are displayed.
6. Verify that the models are clearly distinguishable from one another.
7. Confirm that the app displays the relevant forecast values used for comparison.
8. Verify that the app identifies or ranks a winning model based on forecast accuracy.
9. Confirm that the result is understandable without needing to inspect developer tools or raw API responses.

## Interaction testing

Interact with the major controls on the page.

Verify that:

- Location input works.
- Buttons respond when clicked.
- Loading states appear appropriately while weather data is being fetched.
- Results update when a different location is entered.
- The UI remains usable after multiple comparisons.
- Links, tabs, dropdowns, or other interactive elements work as expected.

## Edge cases

Try at least one invalid or unusual input, such as:

- An invalid location.
- An empty location.
- A location that may not have available weather data.

Confirm that the application handles the situation gracefully and provides a useful error or empty state instead of crashing.

## Visual inspection

Check that:

- Text is readable.
- Important information is not clipped or overlapping.
- Model comparisons are visually understandable.
- The winning model is clearly indicated.
- The interface feels intentional rather than exposing raw API data.

## Final report

Report:

1. Tests performed.
2. Tests that passed.
3. Tests that failed.
4. Any console, network, or visible application errors encountered.
5. Any usability problems discovered.
6. The exact steps needed to reproduce each failure.

Finish with one of these verdicts:

**PASS** — The primary Weather Model Fight Club flow works end-to-end.

**PASS WITH ISSUES** — The primary flow works, but non-blocking problems were discovered.

**FAIL** — A problem prevents the primary flow from completing.
